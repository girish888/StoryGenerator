spring.application.name=StoryGenerator
server.port=8080

# Database (your values)
spring.datasource.url=jdbc:mysql://localhost:3306/StoryGenerator?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Girishuday@888
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Read API key from environment variable AI_GEMINI_KEY (tolerant default)
ai.gemini.key=${AI_GEMINI_KEY:}

# Recommended Model Version
spring.ai.google.genai.chat.options.model=gemini-2.0-flash

# If you have vertex auto-configuration problems, disable it (optional)
spring.autoconfigure.exclude=org.springframework.ai.autoconfigure.vertexai.gemini.VertexAiGeminiAutoConfiguration

# Server Configuration
server.port=9090

# Security (dev)
spring.security.user.name=admin
spring.security.user.password=password
